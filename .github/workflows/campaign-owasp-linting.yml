name: Campaign - OWASP Linting

on:
  workflow_dispatch:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - name: Main branch stub
        run: |
          echo "This is a stub on main."
          echo "Run workflow from feature branch ref for full implementation."
