name: Meta-Release Collector v3 (Stub)
# Full implementation available in branch: feat/meta-release-collector-v3

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Information
        run: |
          echo "=================================================="
          echo "This is a stub workflow for Meta-Release Collector v3"
          echo "=================================================="
          echo ""
          echo "To use the full v3 workflow:"
          echo "1. Switch to branch: feat/meta-release-collector-v3"
          echo "2. Run the workflow from that branch"
          echo ""
          echo "For testing with disposable data:"
          echo "- Use branch: test/meta-release-collector-v3"
          echo ""
          echo "Full workflow file: .github/workflows/meta-release-collector.yml"
          echo "Test plan: See private-dev-docs/project-administration/workflow-v3-planning/test-plan.md"